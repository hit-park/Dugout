spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dugout
    username: dugout
    password: dugout
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379
